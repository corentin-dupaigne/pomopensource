apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pomopensource.fullname" . }}-test-connection"
  labels:
    {{- include "pomopensource.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pomopensource.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
