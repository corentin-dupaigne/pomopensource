Application deployed.

URL:  {{ .Values.app.url }}

The HTTPRoute "{{ include "pomopensource.fullname" . }}-http-route" is attached to
gateway "{{ .Values.gateway.namespace }}/{{ .Values.gateway.name }}" with hostname
"{{ .Values.hostname }}".

Inspect with:
  kubectl get -n {{ .Release.Namespace }} httproute,svc,deploy -l app.kubernetes.io/instance={{ .Release.Name }}
  kubectl get -n {{ .Values.gateway.namespace }} gateway/{{ .Values.gateway.name }}
