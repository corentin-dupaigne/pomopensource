apiVersion: v2
name: pomopensource
description: A Helm chart for Kubernetes
type: application
version: 1.1.0
appVersion: "1.0.0"

annotations:
  org.opencontainers.image.source: https://github.com/corentin-dupaigne/pomopensource

dependencies:
  - name: mysql
    version: "11.x.x"
    repository: "https://charts.bitnami.com/bitnami"
    condition: mysql.enabled
